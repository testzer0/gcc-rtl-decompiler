/*-------------------------------------------------------*
 | scanner.h 											 |
 | ---------											 |
 | This file defines the tokens and the enum type used   |
 | our scanner, and declares a few exported functions.	 |
 *-------------------------------------------------------*/

#ifndef SCANNER_H_

#define SCANNER_H_

#include <stdio.h>

/*-------------------------------------------------------*
 * TokenType :- defines the constants for the different  *
 * tokens used by the scanner. 							 *
 *-------------------------------------------------------*/

typedef enum{

} TokenType;


#endif
